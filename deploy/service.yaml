apiVersion: v1
kind: Service
metadata:
  name: k8s-networking-mcp
  namespace: k8s-networking-mcp
spec:
  selector:
    app: k8s-networking-mcp
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP